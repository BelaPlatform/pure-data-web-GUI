[Unit]
Description=Pd WebGui Shim
DefaultDependencies=no
After=network-pre.target
Conflicts=shutdown.target
Before=shutdown.target

[Service]
ExecStart=/usr/bin/nodejs /opt/pd-webgui/shim/ nopd
Type=simple
Restart=always

[Install]
WantedBy=default.target
